import { use as chaiUse, expect } from 'chai'
import chaiAsPromised from 'chai-as-promised'

chaiUse(chaiAsPromised)

describe('fco-game-engine tournament and match store features', () => {
  it('works', () => {
    expect(1 + 1).to.equal(2)
  })
})
